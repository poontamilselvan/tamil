#include <stdio.h>

int main()
{
    char a[20];
    int n,i;
    printf("enter the name\n");
    scanf("%s",&a[i]);
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
            printf("%c",a[i]);
    }
    return 0;
}
