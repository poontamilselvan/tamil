#include <stdio.h>
#include<string.h>

int main()
{
   char a[20];int i,c=1;
   printf("enter the name\n");
   scanf("%s",&a[i]);
   for(i=0;i<=20;i++)
   {
           if(isspace (a[i]))
           c++;
   }
   printf("%d",c);
    return 0;
}
