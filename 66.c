#include <stdio.h>
#include<string.h>

int main()
{
   int a[10],b
   printf("enter the num");
   scanf("%d",&a[i]);
   b=a[i];
   for(i=2;i<=b;i++)
   {
           if(b%i==0)
           {
           flag=0;
           }
   }
   if(flag==0)
   {
           printf("not prime");
   }
   else
   {
           printf("prime");
   }
   return 0;
}
